const form=document.getElementById('memoForm');const steps=[...document.querySelectorAll('.step')];const nextButtons=[...document.querySelectorAll('.next')];const dots=[...document.querySelectorAll('.step-dots span')];const stepText=document.getElementById('stepText');const progressBar=document.getElementById('progressBar');const statusBox=document.getElementById('status');let currentStep=0;function showStep(i){currentStep=i;steps.forEach((s,n)=>s.classList.toggle('active',n===i));dots.forEach((d,n)=>d.classList.toggle('active',n<=i));stepText.textContent=`${i+1} of 3`;progressBar.style.width=`${((i+1)/3)*100}%`;statusBox.textContent='';statusBox.className='status'}nextButtons.forEach(b=>b.addEventListener('click',()=>{for(const f of steps[currentStep].querySelectorAll('input,textarea')){if(!f.checkValidity()){f.reportValidity();return}}showStep(currentStep+1)}));form.addEventListener('submit',async e=>{e.preventDefault();for(const f of steps[currentStep].querySelectorAll('input,textarea')){if(!f.checkValidity()){f.reportValidity();return}}const button=document.getElementById('resetButton');button.disabled=true;button.textContent='Resetting...';const payload={answer1:document.getElementById('field1').value.trim(),answer2:document.getElementById('field2').value.trim(),answer3:document.getElementById('field3').value.trim(),answer4:document.getElementById('field4').value.trim()};try{const r=await fetch('/api/memos',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const result=await r.json();if(!r.ok)throw new Error(result.message||'Unable to save the form.');window.location.href='reset-success.html'}catch(err){statusBox.className='status error';statusBox.textContent=err.message||'Something went wrong.';button.disabled=false;button.textContent='Reset'}});showStep(0);
